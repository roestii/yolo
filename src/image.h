#ifndef IMAGE_H
#define IMAGE_H

#include "types.h"

void imageToColumns(float*, float*, int, int, int, int);
void imageToRows(float*, float*, int, int, int, int);
void columnsToImage(float*, float*, int, int, int, int);

#endif
